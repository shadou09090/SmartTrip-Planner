# SmartTrip Planner - Frontend (React + Vite)

Este directorio contiene la aplicación cliente construida con **React 18** y **Vite**.

## 🚀 Cómo Iniciar la Aplicación

1. **Abrir la terminal e ingresar a esta carpeta:**
   ```bash
   cd frontend
   ```

2. **Instalar dependencias (solo la primera vez):**
   ```bash
   npm install
   ```

3. **Ejecutar el servidor de desarrollo:**
   ```bash
   npm run dev
   ```

4. **Acceder a la aplicación:**
   Abre [http://localhost:3000](http://localhost:3000) en tu navegador.

---

## 🛠️ Scripts Disponibles

- `npm run dev` - Arranca el servidor de desarrollo local de Vite en el puerto 3000.
- `npm run build` - Genera el bundle optimizado para producción en la carpeta `dist/`.
- `npm run preview` - Sirve localmente los archivos compilados de producción.

---

## 📁 Estructura del Código

```text
src/
├── components/
│   ├── layout/       # Navbar, Footer
│   └── ui/           # InputGroup, Button, FeatureCard
├── pages/            # Home, Login, Register
└── styles/           # Variables, Global, Forms, Navbar, Home CSS
```
